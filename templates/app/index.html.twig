{% extends 'base.html.twig' %}

{% block title %}Hello AppController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/Users/pgrimaud/Documents/dev/iim/a2-symfony/td-1-matin/src/Controller/AppController.php'|file_link(0) }}">src/Controller/AppController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/Users/pgrimaud/Documents/dev/iim/a2-symfony/td-1-matin/templates/app/index.html.twig'|file_link(0) }}">templates/app/index.html.twig</a></code></li>
    </ul>

    <ul>
        {% for category in categories %}
            <li>{{ category.name }} ({{ category.pets|length }})</li>
            <ul>
                {% for pet in category.pets %}
                    <li>{{ pet.name }} - {{ pet.birthDate|date('Y') }}</li>
                {% endfor %}
            </ul>
        {% endfor %}
    </ul>
</div>
{% endblock %}
